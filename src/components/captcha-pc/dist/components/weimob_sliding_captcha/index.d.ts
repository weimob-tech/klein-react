import AbstractComponent from '../abstract';
import './index.less';
declare class Sliding extends AbstractComponent {
    private templateHtml;
    private data;
    private mounted;
    private methods;
    constructor(options: any);
}
export default Sliding;
